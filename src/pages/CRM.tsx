// CRM module removed — product refocused on SEO/SEM/SMM.
// This file is kept as a stub; the route redirects to /analytics.
import { Navigate } from "react-router-dom";
export default function CRM() {
  return <Navigate to="/analytics" replace />;
}
